services:
  nginx:
    restart: always
    build:
      context: .
      dockerfile: .deploy/nginx/nginx.prod.dockerfile
    volumes:
#      - .dev/nginx:/etc/letsencrypt/live/bot-reviewer.ru:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - php
      - cronicle
#      - certbot
    networks:
      - default

  php:
    restart: always
    build:
      context: .
      dockerfile: .deploy/php/php.prod.Dockerfile
    depends_on:
      - pgsql
      - redis
    networks:
      - default


  pgsql:
    image: postgres:17-alpine
    restart: always
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - database:/var/lib/postgresql/data
    networks:
      - default

  redis:
    image: redis:7-alpine
    networks:
      - default

  cronicle:
    image: bluet/cronicle:latest
    container_name: cronicle
    restart: always
    volumes:
      - cronicle_data:/opt/cronicle/data
      - cronicle_logs:/opt/cronicle/logs
    environment:
      TZ: Europe/Moscow
    ports:
      - "3012:3012" # web UI внутри сети
    networks:
      - default

  certbot:
    build:
      context: .
      dockerfile: .deploy/certbot/Dockerfile
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
      - ./public:/var/www/certbot
    healthcheck:
      test: [ "CMD", "test", "-f", "/etc/letsencrypt/live/bot-reviewer.ru/fullchain.pem" ]
      interval: 1m
      retries: 5
    networks:
      - default

networks:
  default:
    driver: bridge

volumes:
  database:
    driver: local
  cronicle_data:
    driver: local
  cronicle_logs:
    driver: local
